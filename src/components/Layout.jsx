import {Link, Outlet} from 'react-router-dom';
import AppNavbar from "./AppNavbar";


const Layout = () => {
    return (
        <div>
            <AppNavbar />
        </div>
    )
}

export default Layout;